<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="login.css")
        title login Sedme
    
    body.loginScreen
        header.navBar
            p.title SEDME 
        .mainContainer
            img.mainLogo(src="img/logoPM.jpeg", alt="")
            p.user User:
            input.userInput(type="text")
            p.password Password:
            input.passwordInput(type="text")
            button.enterButton Entrar
            a.passwordRecover(href="") Recuperar Contraseña
        footer.footerBar 
            p.title Sistema Estudiantil de Definición de Metas para Platzi Master